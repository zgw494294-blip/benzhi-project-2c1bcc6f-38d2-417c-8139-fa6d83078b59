# 典藏扫描品发布核准台

面向档案管理员、文物保护员和发布复核员的扫描档案质量核准工作台。系统支持案卷建档、不可变修订、页级规则发现、整改复验、独立复核、冻结发布，以及 HMAC 凭据验证。

## 构建、运行与测试

```text
go test ./...
go run . -addr=127.0.0.1:19081
go run . -addr=127.0.0.1:19081 -selfcheck
```

可通过 `PORT` 环境变量或 `-addr` 配置监听地址，数据默认写入 `./data`，凭据密钥读取 `RELEASE_HMAC_KEY`。
